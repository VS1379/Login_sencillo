<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="stylesheet" href="/css/register.css">

    <title>Login</title>
</head>

<body>
    <form class="register" action="/register" method="post" enctype="multipart/form-data">

        <div><label>Ingresa tu Nombre</label>
            <input type="text" name="name"></input>
            <div class="error">
                <% if (locals.errors && locals.errors.name) { %>
                    <%= errors.name.msg%>
                        <% } %>
            </div>
        </div>
        <div><label>Ingresa tu Apellido</label>
            <input type="text" name="lastName"></input>
            <div class="error">
                <% if (locals.errors && locals.errors.lastName) { %>
                    <%= errors.lastName.msg%>
                        <% } %>
            </div>
        </div>
        <div><label>Ingresa tu email</label>
            <input type="email" name="email"></input>
            <div class="error">
                <% if (locals.errors && locals.errors.email) { %>
                    <%= errors.email.msg%>
                        <% } %>
            </div>
        </div>
        <div><label>Ingresa una Contraseña</label>
            <input type="password" name="password"></input>
            <div class="error">
                <% if (locals.errors && locals.errors.password) { %>
                    <%= errors.password.msg%>
                        <% } %>
            </div>
        </div>
        <div class="buttons">
            <button type="submit">Ingresar</button>
            <button type="reset">Cancelar</button>
        </div>
    </form>

</body>

</html>